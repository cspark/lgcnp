<html>
  <head>
    <script type="text/javascript" src="js/jquery.js"></script>
    <script type="text/javascript" src="js/bootstrap.js"></script>
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="https://code.jquery.com/ui/1.10.0/themes/base/jquery-ui.css" type="text/css"/>
    <script src="https://code.jquery.com/ui/1.10.0/jquery-ui.min.js"></script>

    <script>
      $(function () {
        $(document).keyup(function (e) {
            if ($("#search").is(":focus") && (e.keyCode == 13)) {
                console.log(encodeURI($("#search").val()));
                location.href = "/user_list?search=" + encodeURI(encodeURI($("#search").val()));
            }
        });
      });
    </script>
  </head>

  <body>
    <h2>고객정보</h2>
    <div>
      <input type="text" name="search" id="search"> 현재는 이름으로 검색이 가능합니다
    </div>
    <div>

      <% @users.each do |user| %>
        <%= render partial: "user/item", locals: { user: user } %>
      <% end %>
      <div>
        <%= paginate @users %>
      </div>
    </div>
  </body>
</html>
