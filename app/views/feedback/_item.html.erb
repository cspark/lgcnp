<!DOCTYPE html>
<html>
    <head>
      <script>
        $(function () {
          <% user = Custinfo.find tabletinterview.custserial %>
          try
          {
            $('#name_<%= user.custserial.to_i %>').append(decode_utf8('<%= user.custname %>'));
          }
          catch(err)
          {
            $('#name_<%= user.custserial.to_i %>').append('<%= user.custname %>');
          }
        });

        function decode_utf8(s) {
          return decodeURI(s);
        }
      </script>
    </head>
    <body>
      <div style="display:table; height:50px; width: 100%; float:left; background: #F0F8FF; margin: 2px; padding: 8px;">
        <% custinfo = Custinfo.find tabletinterview.custserial %>
        <font size="4"><div id="name_<%= user.custserial.to_i %>"></div></font>
        시리얼 : <%= tabletinterview.custserial %> / 진단 날짜 : <%= tabletinterview.uptdate %>
      </div>
    </body>
</html>
